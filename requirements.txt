SQLAlchemy>=2.0
psycopg2-binary
requests
beautifulsoup4
lxml
anthropic>=0.40
streamlit==1.46.0
starlette==0.46.2
pandas
python-dotenv
yfinance>=0.2
fastapi>=0.100
uvicorn==0.34.3
pydantic>=2.0
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
python-multipart>=0.0.6
email-validator>=2.0
pydantic-settings>=2.0
apscheduler>=3.10
# For narrative silence-decay reconfirmation (NARRATIVE_SPEC Phase 1b).
# NOTE: also satisfies the legacy embeddings step, which is deliberately
# PARKED in scheduler_light step 5 — do not un-park it outside the
# Phase 2 freeze ritual (V3_FIXLIST #0: it feeds live scoring's P leg).
sentence-transformers>=2.2


