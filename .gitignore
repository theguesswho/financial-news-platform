# Environment secrets
.env
env.yaml

# State files
seen_filings.txt

# Python
venv/
__pycache__/
*.pyc

# Local database and dumps — machine state, never code.
# Railway runs its own Postgres; nothing deployed reads this folder.
data/
